#include <bits/stdc++.h>

#define FAST ios_base::sync_with_stdio(0); std::cin.tie(0); std::cout.tie(0);
typedef long long ll;

using namespace std;


int main()
{
	FAST;
	int n; cin >> n;
	if (n != 1 && n != 3)
	{
		cout << "YES";
	}
	else
	{
		cout << "NO";
	}


	

	return 0;
}

