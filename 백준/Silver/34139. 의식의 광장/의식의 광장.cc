#include<bits/stdc++.h>

#define FAST ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define ll long long

using namespace std;


int main()
{
	FAST;
	int h, n; cin >> h >> n;

	for (int i = 0; i < n; i++)
	{
		int a, b; cin >> a >> b;
	}

	cout << "YES\n";

	for (int i = 0; i < n; i++)
	{
		cout << i + 1 << " ";
	}
}